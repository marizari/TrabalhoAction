name: Listar diretório completo

on:
  workflow_dispatch:

jobs:
  listar:
    runs-on: ubuntu-latest

    steps:
      - name: Baixar código do repositório
        uses: actions/checkout@v4

      - name: Listar arquivos e pastas
        run: |
          echo "Arquivos e pastas no diretório atual:"
          ls -la
